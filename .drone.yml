kind: pipeline
name: tahokas-testing 

steps:
  - name: tester
    image: alpine 
    commands:
      - echo moi > /asd/test
      - pwd
    volumes:
      - name: asd
        path: /asd
  - name: tester2
    image: alpine
    commands:
      - cat /asd/test
    volumes:
      - name: asd
        path: /asd

services:
  - name: server
    image: nginx:alpine

volumes:
  - name: asd
    temp: {}
